name: "Publish on Release"

on:
  release:
    types:
      - published
  workflow_dispatch:

jobs:
  build:
    uses: decaf-ts/reusable-actions/.github/workflows/publish-on-release.yaml@main
    secrets: inherit
